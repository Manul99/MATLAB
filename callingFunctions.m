Random = magic(4);

% Returns the maximum elements of an array.
Max = max(Random);

% Returns the minimum element of first column.
Min_FirstCol = max(Random(:,1));

% Returns the minimum element of row.
Min_FirstRow = min(Random(1,:));

% Return the row vector whose elements are the lengths of the corresponding
% dimensions of matrix
Mat_Size = size(Random);

A = [100 34 65 78 22 12 9];

% Find the mean of A array
mean_dataset = mean(A);

% Let's craete new vector to implement variance and standard_deviation
vector = [2,1.8,1.5,1.9];
Variance = var(vector);
standard_deviation = std(vector);

x = [0 2 9 2 5 8 7 3 1 9 4 3 5 8 10 0 1 2 9 5 10];
Variance1 = var(x);
standard_deviation1 = std(x);

%hist(x)
%nbis = 5;
%hist(x,nbis)


% eki laga bag ekak na bn 
% meki arakita  keewada manda 

% awlak na hostel podi un bn 
%yathuru wagaya thibba bn oya mahatha eki gawa

%apu gmn bn whutti orawagena hitiye
%nikn puka denna one na ne
%habai lagata lassanai ne

set bh



